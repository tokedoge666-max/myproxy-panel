# MyProxy Panel v1.0

## 个人代理节点管理系统技术设计与 Codex 实施规格

------

# 1. 项目背景

目标服务器环境：

```text
操作系统：
Ubuntu 20.04.6 LTS
Provider Image:
ubuntu200406S

服务器：
美国洛杉矶
双 ISP 住宅线路
4837

硬件：
1 vCPU
1 GB RAM

用途：
仅供服务器所有者个人使用
```

需要开发一套轻量级 Web 管理系统，用于管理个人 sing-box 代理节点，并生成可直接导入 Mihomo / Clash Meta 兼容客户端的订阅。

项目名称暂定：

```text
MyProxy Panel
```

------

# 2. 项目目标

系统需要实现：

```text
Web 管理面板

sing-box 管理

Hysteria2

TUIC

Shadowsocks 2022

节点启停

节点参数修改

安全随机密钥生成

sing-box 配置自动生成

配置检查

配置安全应用

配置失败自动回滚

Mihomo / Clash Meta Subscription

Proxy Provider Subscription

系统资源监控

sing-box 日志查看

一键部署

一键更新

一键卸载
```

第一版只针对：

```text
单服务器
单管理员
个人使用
```

不实现机场商业功能。

------

# 3. 核心设计原则

整个系统采用：

```text
Control Plane
+
Data Plane
```

架构。

其中：

```text
FastAPI
=
Control Plane

sing-box
=
Data Plane
```

FastAPI 只负责：

```text
配置管理
节点管理
订阅生成
系统状态
配置生成
服务控制
```

实际代理协议全部交给：

```text
sing-box
```

实现。

禁止自行实现：

```text
Hysteria2
TUIC
Shadowsocks
VLESS
```

等协议。

------

# 4. 总体架构

```text
                       Browser
                          │
                          │ HTTPS
                          ▼
                     ┌─────────┐
                     │  Nginx  │
                     └────┬────┘
                          │
             ┌────────────┼────────────┐
             │            │            │
             ▼            ▼            ▼
          React         /api/*       /sub/*
        Static UI          │            │
                           ▼            ▼
                       FastAPI      FastAPI
                           │            │
                           │            ▼
                           │        Subscription
                           │          Generator
                           │
                  ┌────────┴────────┐
                  │                 │
                  ▼                 ▼
               SQLite         Config Generator
                                    │
                                    ▼
                             sing-box config
                                    │
                                    ▼
                              config check
                                    │
                                    ▼
                              sing-box core
                                    │
                       ┌────────────┼────────────┐
                       │            │            │
                       ▼            ▼            ▼
                 Hysteria2       TUIC       SS2022
                       │            │            │
                       └────────────┴────────────┘
                                    │
                                    ▼
                           LA Residential IP
```

------

# 5. 技术栈

## Backend

使用：

```text
Python 3.12

FastAPI

Uvicorn

SQLAlchemy 2.x

Pydantic 2.x

SQLite

psutil

PyYAML

argon2 / passlib
```

Python 依赖管理：

```text
uv
```

------

# 6. Frontend

使用：

```text
React

TypeScript

Vite

Ant Design

Axios
```

第一版：

```text
不使用 Redux
不使用复杂状态管理
不使用 SSR
```

生产环境只部署：

```text
frontend/dist
```

静态资源。

------

# 7. Proxy Core

使用：

```text
sing-box Stable
```

禁止：

```text
Beta
第三方魔改 Core
不明来源二进制
```

系统需要记录：

```bash
sing-box version
```

输出。

所有配置必须通过：

```bash
sing-box check
```

验证。

------

# 8. 最高优先级：项目环境隔离

本项目必须采用：

```text
Project-local
Self-contained
```

模式。

原则：

> Python、Python 依赖、Node.js、Node 依赖、sing-box、数据库、配置、日志、临时文件、备份等尽量全部保存在项目目录中。

禁止污染 Ubuntu 全局运行环境。

------

# 9. 系统 Python 禁止修改

Ubuntu 20.04 自带 Python：

```text
必须保持不变
```

禁止：

```bash
apt install python3.12

update-alternatives --config python

ln -sf ... /usr/bin/python

ln -sf ... /usr/bin/python3

sudo pip install ...

pip install --system ...
```

不得修改：

```text
/usr/bin/python

/usr/bin/python3
```

------

# 10. 项目根目录

生产环境统一安装：

```text
/opt/myproxy
```

目录：

```text
/opt/myproxy/

├── .runtime/
│   ├── uv/
│   ├── node/
│   ├── sing-box/
│   └── tools/
│
├── backend/
│   ├── .venv/
│   ├── pyproject.toml
│   ├── uv.lock
│   └── app/
│
├── frontend/
│   ├── node_modules/
│   └── dist/
│
├── config/
│   ├── app.env
│   ├── sing-box.json
│   └── nginx.conf
│
├── data/
│   └── myproxy.db
│
├── logs/
│   ├── api.log
│   ├── audit.log
│   └── sing-box.log
│
├── backups/
│   └── sing-box/
│
├── run/
│
├── deploy/
│
├── scripts/
│
└── myproxy
```

------

# 11. Python Runtime

Python 使用：

```text
Python 3.12
```

但不能替换系统 Python。

由 uv 管理项目自己的 Python Runtime。

Backend 虚拟环境：

```text
/opt/myproxy/backend/.venv
```

Python 包只能安装到：

```text
backend/.venv
```

生产命令必须显式使用：

```text
/opt/myproxy/backend/.venv/bin/python
```

或者：

```text
/opt/myproxy/backend/.venv/bin/uvicorn
```

------

# 12. uv 使用

必须包含：

```text
backend/pyproject.toml

backend/uv.lock
```

开发：

```bash
uv sync
```

生产：

```bash
uv sync --frozen --no-dev
```

禁止：

```bash
sudo pip install
pip install --user
```

------

# 13. Node.js 环境隔离

Node.js 不全局安装。

禁止：

```bash
apt install nodejs

apt install npm

npm install -g ...
```

Node Runtime 存放：

```text
/opt/myproxy/.runtime/node/
```

例如：

```text
.runtime/node/bin/node

.runtime/node/bin/npm

.runtime/node/bin/corepack
```

部署脚本临时设置：

```bash
export PATH="/opt/myproxy/.runtime/node/bin:$PATH"
```

禁止修改：

```text
/etc/profile

/etc/environment

/etc/profile.d/*

~/.bashrc
```

------

# 14. Frontend 依赖

所有前端依赖：

```text
frontend/node_modules/
```

构建：

```bash
cd frontend

npm ci

npm run build
```

生产运行：

```text
frontend/dist
```

由 Nginx 提供。

生产环境禁止运行：

```text
vite dev server

npm run dev
```

------

# 15. sing-box 本地化

不要：

```bash
apt install sing-box
```

不要复制到：

```text
/usr/bin

/usr/local/bin
```

sing-box 存放：

```text
/opt/myproxy/.runtime/sing-box/sing-box
```

所有命令使用绝对路径：

```bash
/opt/myproxy/.runtime/sing-box/sing-box version
```

检查配置：

```bash
/opt/myproxy/.runtime/sing-box/sing-box check \
-c /opt/myproxy/config/sing-box.json
```

运行：

```bash
/opt/myproxy/.runtime/sing-box/sing-box run \
-c /opt/myproxy/config/sing-box.json
```

------

# 16. SQLite

数据库：

```text
/opt/myproxy/data/myproxy.db
```

禁止安装：

```text
MySQL

PostgreSQL

Redis
```

第一版仅使用 SQLite。

------

# 17. 配置文件

主环境配置：

```text
/opt/myproxy/config/app.env
```

示例：

```text
MYPROXY_ENV=production

MYPROXY_HOME=/opt/myproxy

MYPROXY_DB=/opt/myproxy/data/myproxy.db

MYPROXY_SINGBOX=/opt/myproxy/.runtime/sing-box/sing-box

MYPROXY_SINGBOX_CONFIG=/opt/myproxy/config/sing-box.json

MYPROXY_LOG_DIR=/opt/myproxy/logs

MYPROXY_BACKUP_DIR=/opt/myproxy/backups
```

权限：

```bash
chmod 600 /opt/myproxy/config/app.env
```

------

# 18. 默认代理协议

v1 支持：

```text
Hysteria2

TUIC

Shadowsocks 2022
```

后期可以扩展：

```text
VLESS Reality

Trojan

AnyTLS
```

但 v1 不需要。

------

# 19. 默认端口

建议：

```text
22/tcp
SSH

80/tcp
HTTP / ACME

443/tcp
Web Panel / Subscription HTTPS

8443/udp
Hysteria2

10443/udp
TUIC

8388/tcp
Shadowsocks

8388/udp
Shadowsocks
```

所有节点端口：

```text
允许后台修改
```

------

# 20. Hysteria2

默认主节点。

名称：

```text
LA-HY2
```

默认：

```text
Port:
8443 UDP

TLS:
Enabled

Obfs:
salamander

Password:
安全随机生成
```

不要默认设置：

```text
up_mbps

down_mbps
```

除非用户主动配置。

配置结构根据服务器实际 sing-box stable 版本生成。

类似：

```json
{
  "type": "hysteria2",
  "tag": "hy2-in",
  "listen": "::",
  "listen_port": 8443,
  "obfs": {
    "type": "salamander",
    "password": "<secure-random>"
  },
  "users": [
    {
      "name": "admin",
      "password": "<secure-random>"
    }
  ],
  "tls": {
    "enabled": true,
    "certificate_path": "<cert>",
    "key_path": "<key>"
  }
}
```

Codex 不得直接照抄。

必须以当前 sing-box：

```text
stable version
```

实际校验结果为准。

------

# 21. TUIC

默认备用节点。

名称：

```text
LA-TUIC
```

默认：

```text
Port:
10443 UDP

UUID:
UUID v4

Password:
Secure Random

Congestion Control:
bbr

Zero RTT:
false

Heartbeat:
10s

TLS:
enabled
```

默认：

```text
zero_rtt_handshake = false
```

------

# 22. Shadowsocks 2022

名称：

```text
LA-SS2022
```

默认协议：

```text
2022-blake3-aes-128-gcm
```

默认端口：

```text
8388
```

支持：

```text
TCP
UDP
```

密码必须：

```text
16 byte secure random key
Base64 encoded
```

禁止：

```text
123456

password

admin

test123
```

等弱密码。

------

# 23. TLS

生产模式建议使用：

```text
panel.example.com
```

域名解析：

```text
A Record
    ↓
VPS IPv4
```

证书：

```text
Let's Encrypt
```

Hysteria2 与 TUIC：

```text
可以共用服务器 TLS 证书
```

------

# 24. 无域名开发模式

如果暂时无域名：

允许：

```text
Self-signed Certificate
```

此时必须显示：

```text
Development / Self Signed Mode
```

Mihomo 配置：

```yaml
skip-cert-verify: true
```

生产正式证书：

```yaml
skip-cert-verify: false
```

------

# 25. sing-box 配置生成机制

禁止 FastAPI 直接无校验覆盖：

```text
config/sing-box.json
```

正确流程：

```text
用户修改节点
      ↓
SQLite 保存新状态
      ↓
Config Generator
      ↓
生成 staging config
      ↓
sing-box check
      ↓
       OK?
   ┌────┴────┐
   │         │
   否         是
   │         │
返回错误     Backup
             │
             ▼
         Atomic Replace
             │
             ▼
        Restart sing-box
             │
             ▼
      Check Service Status
             │
       ┌─────┴─────┐
       │           │
     Failed       OK
       │           │
    Rollback     Success
```

------

# 26. staging

临时配置：

```text
/opt/myproxy/run/sing-box.staging.json
```

检查：

```bash
/opt/myproxy/.runtime/sing-box/sing-box check \
-c /opt/myproxy/run/sing-box.staging.json
```

只有检查成功：

才允许替换正式配置。

------

# 27. 配置备份

正式配置：

```text
/opt/myproxy/config/sing-box.json
```

修改前备份：

```text
/opt/myproxy/backups/sing-box/
```

命名：

```text
config-YYYYMMDD-HHMMSS.json
```

最多：

```text
20
```

份。

超出后删除最旧备份。

------

# 28. 自动回滚

如果重启后：

```bash
systemctl is-active myproxy-singbox
```

不是：

```text
active
```

则：

```text
恢复上一份 config

restart

再次检查

记录 audit log

返回失败原因
```

绝对不能因为一次 Web 配置错误造成节点永久失联。

------

# 29. Linux 用户权限

创建：

```text
myproxy
```

Linux 用户。

Backend：

```text
User=myproxy
Group=myproxy
```

禁止 Backend：

```text
以 root 用户运行
```

------

# 30. 权限原则

应用目录：

```text
/opt/myproxy
```

归属于：

```text
myproxy
```

敏感配置：

```text
600
```

数据库：

```text
600 / 640
```

------

# 31. systemd

允许 systemd 作为：

```text
进程守护器
```

但 Runtime 必须来自项目目录。

需要两个 Service：

```text
myproxy-api.service

myproxy-singbox.service
```

------

# 32. FastAPI systemd

示例：

```text
User=myproxy

WorkingDirectory=/opt/myproxy

ExecStart=/opt/myproxy/backend/.venv/bin/uvicorn \
app.main:app \
--app-dir /opt/myproxy/backend \
--host 127.0.0.1 \
--port 8000

Restart=on-failure
```

FastAPI：

```text
只允许监听 127.0.0.1
```

禁止：

```text
0.0.0.0:8000
```

公网暴露。

------

# 33. sing-box systemd

启动路径：

```text
/opt/myproxy/.runtime/sing-box/sing-box
```

配置：

```text
/opt/myproxy/config/sing-box.json
```

ExecStart：

```text
/opt/myproxy/.runtime/sing-box/sing-box run \
-c /opt/myproxy/config/sing-box.json
```

------

# 34. Backend 数据模型

## Admin

```text
id

username

password_hash

created_at

last_login
```

只支持：

```text
1 个管理员
```

禁止公开注册。

------

# 35. Settings

```text
id

server_name

server_ipv4

domain

certificate_path

private_key_path

subscription_token

created_at

updated_at
```

------

# 36. ProxyNode

```text
id

name

protocol

enabled

listen_port

config_json

created_at

updated_at
```

protocol：

```text
hysteria2

tuic

shadowsocks
```

协议专属配置：

```text
config_json
```

保存。

------

# 37. AuditLog

字段：

```text
id

action

detail

created_at
```

记录：

```text
Login

Logout

Node Created

Node Updated

Secret Regenerated

Apply Config

Restart

Rollback

Config Validation Failed
```

禁止记录：

```text
完整 Password

完整 Token

完整 Private Key

完整 Subscription URL
```

------

# 38. Authentication

接口：

```text
POST /api/v1/auth/login

POST /api/v1/auth/logout

GET /api/v1/auth/me
```

管理员密码使用：

```text
Argon2id
```

Cookie：

```text
HttpOnly

Secure

SameSite=Strict
```

不要长期把：

```text
Admin Token
```

保存到：

```text
localStorage
```

------

# 39. System API

```text
GET /api/v1/system/status
```

返回：

```json
{
  "cpu_percent": 12.3,
  "memory_percent": 31.2,
  "memory_used_mb": 320,
  "memory_total_mb": 1024,
  "disk_percent": 20.1,
  "uptime_seconds": 123456,
  "singbox": {
    "status": "running",
    "version": "..."
  }
}
```

使用：

```text
psutil
```

读取。

------

# 40. Node API

```text
GET    /api/v1/nodes

GET    /api/v1/nodes/{id}

POST   /api/v1/nodes

PUT    /api/v1/nodes/{id}

DELETE /api/v1/nodes/{id}

POST   /api/v1/nodes/{id}/enable

POST   /api/v1/nodes/{id}/disable

POST   /api/v1/nodes/{id}/regenerate-secret
```

------

# 41. 默认节点

首次初始化自动创建：

```text
LA-HY2

LA-TUIC

LA-SS2022
```

------

# 42. sing-box API

```text
GET  /api/v1/singbox/status

POST /api/v1/singbox/check

POST /api/v1/singbox/apply

POST /api/v1/singbox/restart

GET  /api/v1/singbox/logs
```

logs：

```text
默认最近 200 行
```

------

# 43. Subscription API

核心接口：

```text
GET /sub/{token}
```

默认：

```text
Mihomo YAML
```

预留：

```text
GET /sub/{token}?format=mihomo

GET /sub/{token}?format=provider

GET /sub/{token}?format=singbox
```

v1 必须实现：

```text
mihomo

provider
```

------

# 44. Subscription Token

生成：

```text
至少 32 byte secure random
```

Python：

```python
secrets.token_urlsafe(...)
```

禁止：

```text
random.random

timestamp

username

固定 UUID
```

------

# 45. Token Rotate

后台提供：

```text
Rotate Subscription Token
```

执行后：

```text
旧 Token 立即失效
```

------

# 46. Mihomo Subscription

生成类似：

```yaml
mixed-port: 7890

allow-lan: false

mode: rule

log-level: info

ipv6: false

proxies:

  - name: LA-HY2
    type: hysteria2
    server: <server>
    port: 8443
    password: <password>
    obfs: salamander
    obfs-password: <obfs-password>
    sni: <domain>
    skip-cert-verify: false

  - name: LA-TUIC
    type: tuic
    server: <server>
    port: 10443
    uuid: <uuid>
    password: <password>
    sni: <domain>
    skip-cert-verify: false
    congestion-controller: bbr

  - name: LA-SS2022
    type: ss
    server: <server>
    port: 8388
    cipher: 2022-blake3-aes-128-gcm
    password: <password>
    udp: true

proxy-groups:

  - name: Proxy
    type: select
    proxies:
      - LA-HY2
      - LA-TUIC
      - LA-SS2022
      - DIRECT

rules:

  - MATCH,Proxy
```

注意：

```text
最终字段必须根据当前 Mihomo 官方配置格式校验。
```

------

# 47. 节点选择

v1 默认：

```text
select
```

也就是：

```text
手动选择节点
```

不要默认：

```text
url-test

fallback

load-balance
```

以后可以增加。

------

# 48. Proxy Provider

接口：

```text
/sub/{token}?format=provider
```

只返回：

```yaml
proxies:
  ...
```

方便添加到已有 Mihomo：

```text
proxy-providers
```

配置中。

------

# 49. Nginx

Nginx 负责：

```text
HTTPS

React Static Files

/api Proxy

/sub Proxy
```

公网访问：

```text
https://domain/
```

API：

```text
https://domain/api/
```

Subscription：

```text
https://domain/sub/<token>
```

FastAPI：

```text
127.0.0.1:8000
```

------

# 50. Subscription 日志安全

Nginx：

```text
/sub/
```

必须：

```text
access_log off
```

防止：

```text
subscription token
```

写入日志。

FastAPI 日志：

不得打印：

```text
完整订阅 URL
```

------

# 51. Web 页面

第一版只需要：

```text
Login

Dashboard

Nodes

Subscription

Settings
```

------

# 52. Login

字段：

```text
Username

Password

Login
```

首次登录：

```text
强制修改随机初始密码
```

------

# 53. Dashboard

显示：

```text
Server Status

CPU

RAM

Disk

Uptime

sing-box Status

Hysteria2 Status

TUIC Status

Shadowsocks Status
```

示例：

```text
MyProxy Panel

CPU
12%

RAM
320 / 1024 MB

Disk
21%

Uptime
3d 14h

sing-box
● Running

Hysteria2
● Running

TUIC
● Running

Shadowsocks
● Running
```

按钮：

```text
Restart sing-box

View Logs
```

------

# 54. Nodes 页面

示例：

```text
LA-HY2

Protocol
Hysteria2

Port
8443 UDP

Enabled
Yes

[Edit]

[Disable]

[Regenerate Password]
```

另外两个同理。

------

# 55. Subscription 页面

展示：

```text
Mihomo Subscription

https://domain/sub/xxxxxxxx
```

按钮：

```text
Copy

QR Code

Preview

Rotate Token
```

同时显示：

```text
Proxy Provider URL
```

------

# 56. Settings 页面

设置：

```text
Server IPv4

Domain

Certificate Path

Private Key Path
```

危险操作：

```text
Restart sing-box

Regenerate All Credentials

Restore Backup
```

危险操作需要：

```text
二次确认
```

------

# 57. UI 风格

要求：

```text
简洁

深色模式

技术 Dashboard 风格

响应式

低资源
```

不要：

```text
3D

大量动画

复杂渐变

超大依赖
```

------

# 58. Health Check

公开接口：

```text
GET /health
```

只返回：

```json
{
  "status": "ok"
}
```

不要公开：

```text
CPU

RAM

节点信息

版本

系统信息
```

详细状态必须登录。

------

# 59. 防火墙

允许：

```text
22/tcp

80/tcp

443/tcp

8443/udp

10443/udp

8388/tcp

8388/udp
```

禁止开放：

```text
8000
```

公网访问。

------

# 60. 云厂商安全组

README 必须明确提醒：

除了服务器本机 Firewall，

服务器供应商后台如果存在：

```text
Security Group
Firewall
ACL
```

也必须开放对应端口。

------

# 61. 网络优化

允许检测：

```text
net.ipv4.tcp_congestion_control
```

可以提供：

```text
Enable BBR
```

可选脚本。

但禁止：

```text
锐速

魔改内核

大量来源不明 sysctl

一键网络优化脚本
```

------

# 62. 日志

项目日志目录：

```text
/opt/myproxy/logs
```

包含：

```text
api.log

audit.log

sing-box.log
```

需要过滤：

```text
password

token

authorization

cookie

private key

完整 UUID

subscription URL
```

------

# 63. 项目 CLI

根目录提供：

```text
/opt/myproxy/myproxy
```

使用方式：

```bash
cd /opt/myproxy

./myproxy status

./myproxy restart

./myproxy logs

./myproxy check

./myproxy backup

./myproxy restore

./myproxy info
```

不要安装：

```text
/usr/local/bin/myproxy
```

------

# 64. CLI Status

例如：

```text
MyProxy Panel

API
Running

sing-box
Running

Nginx
Running

LA-HY2
UDP 8443

LA-TUIC
UDP 10443

LA-SS2022
TCP/UDP 8388
```

------

# 65. 项目源码结构

开发仓库：

```text
myproxy-panel/

├── README.md
├── .env.example
├── .gitignore
├── Makefile
├── myproxy
│
├── backend/
│   ├── pyproject.toml
│   ├── uv.lock
│   │
│   └── app/
│       ├── main.py
│       │
│       ├── api/
│       │   ├── auth.py
│       │   ├── nodes.py
│       │   ├── system.py
│       │   ├── singbox.py
│       │   └── subscription.py
│       │
│       ├── core/
│       │   ├── config.py
│       │   ├── security.py
│       │   └── logging.py
│       │
│       ├── models/
│       │   ├── admin.py
│       │   ├── node.py
│       │   ├── settings.py
│       │   └── audit.py
│       │
│       ├── schemas/
│       │
│       ├── services/
│       │   ├── credentials.py
│       │   ├── singbox_config.py
│       │   ├── singbox_service.py
│       │   ├── subscription.py
│       │   └── system_status.py
│       │
│       └── db/
│
├── frontend/
│   ├── package.json
│   ├── package-lock.json
│   ├── vite.config.ts
│   │
│   └── src/
│       ├── api/
│       ├── components/
│       ├── layouts/
│       ├── pages/
│       └── types/
│
├── deploy/
│   ├── install.sh
│   ├── update.sh
│   ├── uninstall.sh
│   │
│   ├── systemd/
│   │   ├── myproxy-api.service
│   │   └── myproxy-singbox.service
│   │
│   ├── nginx/
│   │   └── myproxy.conf.template
│   │
│   └── scripts/
│       ├── health-check.sh
│       ├── download-runtime.sh
│       └── backup-config.sh
│
├── scripts/
│   └── env.sh
│
└── tests/
    ├── test_auth.py
    ├── test_credentials.py
    ├── test_config_generator.py
    ├── test_subscription.py
    └── test_api.py
```

------

# 66. install.sh

目标命令：

```bash
sudo bash deploy/install.sh
```

执行流程：

```text
1. 检查 root 权限

2. 检查 /etc/os-release

3. 确认 Ubuntu 20.04.6 兼容

4. 检查 CPU 架构

5. 创建 /opt/myproxy

6. 创建 myproxy Linux User

7. 下载项目专用 uv

8. 使用 uv 准备项目 Python 3.12

9. 创建 backend/.venv

10. uv sync --frozen --no-dev

11. 下载项目专用 Node LTS

12. npm ci

13. npm run build

14. 下载官方 sing-box stable binary

15. 创建 config/data/logs/backups/run

16. 初始化 SQLite

17. 生成管理员初始密码

18. 生成 Subscription Token

19. 生成 Hysteria2 Credentials

20. 生成 TUIC Credentials

21. 生成 SS2022 Key

22. 生成初始 sing-box config

23. sing-box check

24. 安装 systemd unit

25. 启动 API

26. 启动 sing-box

27. 配置 Nginx

28. 检查服务状态

29. 输出防火墙要求

30. 输出登录信息
```

------

# 67. 安装脚本禁止操作

禁止：

```bash
apt install python3.12

apt install nodejs

apt install npm

pip install ...

sudo pip install ...

npm install -g ...

cp sing-box /usr/bin/

cp sing-box /usr/local/bin/
```

------

# 68. 最小系统级依赖

可以使用 apt 安装必要系统工具，例如：

```text
curl

wget

ca-certificates

nginx

openssl

ufw

git
```

这些属于：

```text
OS Integration
```

而不是应用 Runtime。

核心原则：

```text
Application Runtime
→ 项目目录

OS Integration
→ 最小化
```

------

# 69. Nginx 系统依赖说明

Nginx 可以使用系统 apt 安装。

原因：

```text
它属于服务器 Web Gateway
```

而不是项目语言 Runtime。

如果未来需要做到完全可移植：

可以进一步把：

```text
Caddy
```

打包到项目 Runtime。

但 v1 不需要。

------

# 70. TLS / Certbot

生产环境可以安装：

```text
certbot
```

作为系统运维工具。

但证书实际路径：

必须写入：

```text
config/app.env
```

并由应用读取。

------

# 71. uninstall.sh

需要提供：

```bash
sudo bash deploy/uninstall.sh
```

执行：

```text
停止 myproxy-api

停止 myproxy-singbox

删除 systemd service

reload systemd

删除 Nginx site

恢复防火墙可选配置

删除 /opt/myproxy
```

卸载后不得遗留：

```text
Python 全局包

npm 全局包

Node 全局 Runtime

sing-box 全局 Binary
```

------

# 72. update.sh

提供：

```bash
sudo bash deploy/update.sh
```

流程：

```text
Backup Database

Backup Config

git pull

uv sync --frozen --no-dev

npm ci

npm run build

检查 sing-box Runtime

数据库迁移

Backend Tests

生成配置

sing-box check

restart services

health check

失败自动恢复
```

------

# 73. Runtime Version Pinning

建议项目保存：

```text
.runtime-versions
```

例如：

```text
PYTHON_VERSION=3.12.x

NODE_VERSION=<LTS>

SINGBOX_VERSION=<stable>
```

部署时：

优先按照锁定版本安装。

升级版本：

通过：

```text
update.sh
```

统一执行。

------

# 74. Python 测试

至少测试：

```text
Admin Authentication

Password Hash

Session

Subscription Token

Node CRUD

Config Generator

Backup

Rollback
```

------

# 75. Credential 测试

测试：

```text
Password Randomness

UUID v4

SS2022 Key Length

Base64 Validation

Subscription Token Length
```

------

# 76. sing-box Config 测试

需要覆盖：

```text
HY2 Enabled

HY2 Disabled

TUIC Enabled

TUIC Disabled

SS Enabled

SS Disabled

Port Change

TLS

Credential Regeneration
```

最终生成配置：

必须真实执行：

```bash
sing-box check
```

------

# 77. Subscription 测试

测试：

```text
Hysteria2 YAML

TUIC YAML

SS2022 YAML

Disabled Node Not Included

Invalid Token → 404

Valid Token → YAML

Provider Output
```

生成 YAML：

必须能被 YAML Parser 正常解析。

------

# 78. Frontend 测试

至少：

```text
npm run build
```

必须成功。

如果配置 ESLint：

```text
npm run lint
```

也必须通过。

------

# 79. 安全要求

必须满足：

```text
Backend 不以 root 运行

FastAPI 只监听 localhost

没有公开注册

Password 使用 Argon2id

Token 使用 secrets

Subscription Token 可轮换

TLS Production Enabled

Token 不进入 Nginx access log

配置先 check

失败自动 rollback

敏感日志脱敏

数据库不公网访问
```

------

# 80. 不需要实现的功能

v1 禁止扩展：

```text
支付

套餐

多用户订阅

邀请

注册

邮箱

Telegram Bot

流量计费

节点销售

多服务器集群

Redis

PostgreSQL

Docker

Kubernetes

Celery

消息队列

复杂 RBAC

机场管理
```

项目定位：

```text
个人代理服务器控制面板
```

------

# 81. 内存控制

服务器只有：

```text
1GB RAM
```

空闲状态目标：

```text
sing-box
+
FastAPI
+
Nginx

< 300 MB RAM
```

React：

只运行静态文件。

Node：

只在构建阶段使用。

------

# 82. 低内存构建

部署时：

不要同时：

```text
npm build
+
Python dependency install
+
其他大型任务
```

应顺序执行。

必要时：

检测可用内存。

构建完成后：

Node 不保持后台进程。

------

# 83. 项目开发模式

Backend：

```bash
cd backend

uv sync

uv run uvicorn app.main:app --reload
```

Frontend：

```bash
cd frontend

npm install

npm run dev
```

本地依赖：

```text
backend/.venv

frontend/node_modules
```

------

# 84. 生产环境运行

生产环境不依赖 Shell 当前 PATH。

全部使用绝对路径。

Backend：

```text
/opt/myproxy/backend/.venv/bin/uvicorn
```

sing-box：

```text
/opt/myproxy/.runtime/sing-box/sing-box
```

Node：

生产运行期间：

```text
不需要
```

------

# 85. README

README 必须包含：

```text
项目介绍

架构

最低硬件要求

Ubuntu 20.04 兼容说明

环境隔离方案

域名配置

TLS

开放端口

服务器安全组

安装

更新

卸载

节点配置

订阅导入

Mihomo / Clash Verge 使用

日志

故障排查

配置恢复

安全说明
```

------

# 86. 常用运维命令

README 提供：

```bash
systemctl status myproxy-api

systemctl restart myproxy-api

journalctl -u myproxy-api -f
```

以及：

```bash
systemctl status myproxy-singbox

systemctl restart myproxy-singbox

journalctl -u myproxy-singbox -f
```

Nginx：

```bash
nginx -t

systemctl reload nginx
```

项目 CLI：

```bash
cd /opt/myproxy

./myproxy status

./myproxy logs

./myproxy check
```

------

# 87. Definition of Done

项目只有满足以下条件：

才算完成。

------

## Backend

```text
FastAPI 正常启动

管理员登录正常

Session 正常

节点 CRUD 正常

SQLite 持久化正常

Subscription 正常
```

------

## Proxy

```text
Hysteria2 Config Valid

TUIC Config Valid

SS2022 Config Valid

sing-box check Passed

sing-box Running
```

------

## Subscription

导入 Mihomo 后出现：

```text
LA-HY2

LA-TUIC

LA-SS2022
```

默认：

```text
Proxy
```

组类型：

```text
select
```

------

## Deployment

全新的：

```text
Ubuntu 20.04.6
```

能够通过：

```bash
git clone <repo>

cd myproxy-panel

sudo bash deploy/install.sh
```

完成部署。

------

## Runtime Isolation

检查：

```text
系统 Python 未改变

无全局 Python Packages

无全局 npm Packages

Node 不安装到 /usr/bin

sing-box 不安装到 /usr/bin

sing-box 不安装到 /usr/local/bin
```

------

## Security

满足：

```text
API 8000 不公网开放

Backend 非 root

Token 不写 access log

Password 不明文

配置失败自动 rollback
```

------

# 88. 最终交付物

必须交付：

```text
完整 Git Repository

README.md

backend/

frontend/

deploy/

tests/

.env.example

pyproject.toml

uv.lock

package.json

package-lock.json

install.sh

update.sh

uninstall.sh

systemd service

Nginx config

sing-box config generator

Subscription generator

MyProxy CLI
```

------

# 89. Codex 开发流程

Codex 必须真正实现。

不要只给：

```text
教程

示例代码

伪代码

架构建议
```

执行：

```text
Phase 1
初始化仓库与目录

Phase 2
Backend Foundation

Phase 3
SQLite Models

Phase 4
Authentication

Phase 5
Credential Generator

Phase 6
sing-box Config Generator

Phase 7
Safe Apply + Backup + Rollback

Phase 8
Subscription Generator

Phase 9
React Panel

Phase 10
CLI

Phase 11
Deployment Scripts

Phase 12
systemd

Phase 13
Nginx

Phase 14
Tests

Phase 15
Security Review

Phase 16
README

Phase 17
Final Integration Test
```

------

# 90. 开发测试原则

每个 Phase 完成后：

立即测试。

禁止：

```text
所有代码写完之后才第一次运行
```

应该：

```text
实现
↓
测试
↓
修复
↓
提交
↓
进入下一阶段
```

------

# 91. sing-box 特殊要求

由于 sing-box 配置可能随版本变化：

Codex 不允许：

```text
直接复制旧博客配置
```

首先确认：

```bash
/opt/myproxy/.runtime/sing-box/sing-box version
```

之后所有生成配置：

```bash
/opt/myproxy/.runtime/sing-box/sing-box check \
-c <generated-config>
```

如果技术文档示例与实际 stable 版本不同：

```text
以当前 stable 官方配置格式为准。
```

不要通过：

```text
降级 sing-box
```

来兼容过时配置。

------

# 92. Mihomo 特殊要求

Subscription Generator：

面向：

```text
Mihomo / Clash Meta Compatible Clients
```

至少保证：

```text
Hysteria2

TUIC

Shadowsocks 2022
```

能够正确导入。

禁止以：

```text
旧版 Clash Premium
```

作为唯一格式标准。

------

# 93. 敏感信息

以下内容：

```text
Server Password

Admin Password

Node Password

UUID

Subscription Token

TLS Private Key
```

不得：

```text
提交 Git

写死源码

输出完整 Debug Log
```

------

# 94. `.gitignore`

至少包含：

```text
.env

config/app.env

*.db

backend/.venv/

frontend/node_modules/

frontend/dist/

.runtime/

logs/

run/

backups/

__pycache__/

.pytest_cache/
```

------

# 95. 最终部署目录边界

最终服务器应该大致为：

```text
Ubuntu 20.04
│
│
├── 系统 Python
│      ↑
│    不修改
│
├── Nginx
│
├── systemd
│
└── /opt/myproxy
       │
       ├── .runtime/
       │     ├── uv
       │     ├── node
       │     └── sing-box
       │
       ├── backend/
       │     └── .venv
       │
       ├── frontend/
       │     └── dist
       │
       ├── config/
       │
       ├── data/
       │
       ├── logs/
       │
       ├── backups/
       │
       └── run/
```

------

# 96. 最重要的禁止事项

以下规则属于：

```text
最高优先级约束
```

禁止：

```text
修改系统 Python

替换 /usr/bin/python3

全局 pip install

sudo pip install

全局 npm install

apt 安装 Node Runtime

将 sing-box 放入 /usr/bin

将 sing-box 放入 /usr/local/bin

Backend 以 root 运行

FastAPI 监听公网

订阅 Token 写入日志

配置未经 sing-box check 就应用

配置失败不回滚
```

------

# 97. 安装完成输出

安装成功后：

输出类似：

```text
========================================

MyProxy Panel Installed Successfully

Panel:

https://panel.example.com

Username:

admin

Temporary Password:

xxxxxxxxxxxxxxxx

Mihomo Subscription:

https://panel.example.com/sub/xxxxxxxxxxxxxxxx

----------------------------------------

Nodes:

LA-HY2
UDP 8443

LA-TUIC
UDP 10443

LA-SS2022
TCP/UDP 8388

----------------------------------------

Services:

API:
Running

sing-box:
Running

Nginx:
Running

========================================
```

并提示：

```text
请保存临时密码

首次登录后修改密码
```

------

# 98. 最终验收测试

Codex 完成开发后必须执行：

```text
Backend Unit Tests

API Tests

Credential Tests

Config Generator Tests

Subscription Tests

Frontend Build

sing-box Config Validation

Service Startup Test

Rollback Test
```

最终提供测试结果。

------

# 99. Codex 最终汇报

完成后必须告诉用户：

```text
1. 完成了哪些功能

2. 项目目录结构

3. Backend 测试结果

4. Frontend Build 结果

5. sing-box Check 结果

6. Subscription 测试结果

7. 安全检查结果

8. 尚未完成的功能

9. Ubuntu 20.04.6 部署步骤

10. 部署前需要用户提供什么参数
```

------

# 100. 部署前需要的参数

最终部署脚本至少需要用户提供：

```text
SERVER_IP

DOMAIN

ACME_EMAIL
```

如果暂时无域名：

支持：

```text
SELF_SIGNED_MODE=true
```

其余：

```text
Admin Password

Subscription Token

HY2 Password

TUIC UUID

TUIC Password

SS2022 Key
```

默认全部：

```text
自动安全生成
```

不需要用户手动填写。

------

# 101. Codex 最终执行指令

现在直接开始开发 MyProxy Panel。

不要只分析。

不要只告诉我怎么写。

直接在当前项目中：

```text
创建文件

实现代码

安装项目内依赖

执行测试

修复错误

完成部署脚本

完成 README
```

严格按照本技术规格实施。

如果规格中的 sing-box 或 Mihomo 示例字段与当前 stable 版本存在差异：

优先检查当前官方配置规范，并通过真实：

```bash
sing-box check
```

验证。

项目最终优先级：

```text
1. 正确

2. 安全

3. 可部署

4. 可恢复

5. 环境隔离

6. 低资源占用

7. 易维护

8. 最后才是功能数量
```

不得为了增加功能破坏：

```text
Ubuntu 全局环境

系统 Python

安全性

配置可恢复性
```

最终目标是在：

```text
Ubuntu 20.04.6
1 vCPU
1GB RAM
```

的服务器上稳定运行：

```text
MyProxy Panel
+
FastAPI
+
React Static UI
+
Nginx
+
sing-box
+
Hysteria2
+
TUIC
+
Shadowsocks 2022
+
Mihomo Subscription
```

并能够通过项目自身：

```text
安装
更新
备份
恢复
卸载
```

完整管理生命周期。